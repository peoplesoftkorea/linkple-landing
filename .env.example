# Mock API(JSON Server)를 쓰려면 이 파일을 .env.local 로 복사하세요.
#   1) npm run server   → http://localhost:3001 에서 db.json이 API로 뜹니다
#   2) npm run dev      → 앱이 localStorage 대신 이 API를 읽고 씁니다
#
# 이 값을 비우거나 파일이 없으면 앱은 localStorage 모드로 동작합니다. (배포본 기본값)
VITE_API_BASE_URL=http://localhost:3001
